buildscript {
    repositories { google(); mavenCentral() }
    dependencies {
        classpath "com.android.tools.build:gradle:7.4.2"
        classpath "org.robovm:robovm-gradle-plugin:2.3.13"
    }
}
allprojects {
    repositories { google(); mavenCentral() }
}